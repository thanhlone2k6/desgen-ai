# Release v8.2.0 - DesignGen AI Pro

## 🎉 Tính năng mới

### 1. Update Dialog Improvements
- Thêm scrollbar cho changelog khi nội dung dài
- Custom scrollbar styling phù hợp với theme
- Có thể cuộn để xem toàn bộ nội dung và vẫn thấy các nút bên dưới

### 2. Image Preview Modal Fix
- Fixed z-index issue - modal hiển thị đúng khi click vào ảnh
- Tăng z-index lên z-[10000] để đảm bảo hiển thị trên tất cả elements
- Improved background visibility

## 🔧 Cải tiến

### 1. UI/UX Improvements
- Update dialog changelog có thể scroll được
- Image preview modal hoạt động ổn định hơn
- Better visual feedback

### 2. GitHub Release Deployment
- Fixed latest.yml file naming để khớp với GitHub upload format
- Improved deployment documentation
- Better error handling

## 🐛 Bug Fixes

- Fixed: Image preview modal không hiển thị khi click vào ảnh (z-index issue)
- Fixed: Update dialog changelog quá dài không có scrollbar
- Fixed: latest.yml file naming không khớp với GitHub upload format

## 📦 Files

- `DesignGen Pro Setup 8.2.0.exe` - Windows installer
- `DesignGen Pro Setup 8.2.0.exe.blockmap` - Block map for updates
- `latest.yml` - Version metadata for auto-update

## 📝 Notes

- Image preview modal bây giờ hoạt động đúng
- Update dialog có scrollbar khi changelog dài
- Auto-update sẽ hoạt động tốt hơn với latest.yml đã được sửa

## 🔄 Migration từ v8.1.0

- Không cần migration đặc biệt
- App sẽ tự động cập nhật khi có phiên bản mới

## 🚀 Installation

1. Download `DesignGen Pro Setup 8.2.0.exe`
2. Run installer
3. Launch app
4. Nhập Google Gemini API key khi được yêu cầu
5. Bắt đầu sử dụng!

---

**By ThanhNg • v8.2.0**

